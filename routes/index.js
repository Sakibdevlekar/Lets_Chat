const {AuthRoutes} = require('./auth.route')
const {ChannelRoutes} =  require('./channel.routs')
const {MessageRoutes} = require ('./message.route')



module.exports = {
    AuthRoutes,
    ChannelRoutes,
    MessageRoutes
    
}